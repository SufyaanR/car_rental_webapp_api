package za.ac.cput.factory;

import java.math.BigDecimal;
import java.time.LocalDate;
import java.time.LocalTime;
import za.ac.cput.domain.RentalProvider;
import za.ac.cput.domain.SubscriptionPayment;

public class SubscriptionFactory {

   /* public SubscriptionPayment newpayment(Long subscriptionPaymentId, RentalProvider rentalProvider, BigDecimal amount, LocalDate paymentDate, LocalTime paymentTime ) {
        return new SubscriptionPayment.SubscriptionBuilder()
        .setSubscriptionPaymentId(subscriptionPaymentId)
        .setRentalProvider(rentalProvider)
        .setAmount(amount)
        .setPaymentDate(paymentDate)
        .setPaymentTime(paymentTime)
        .build();
    }*/
    
}
